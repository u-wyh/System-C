文件类：

FILE* 是 C 标准库的文件流指针，特点：

它不是普通内存，而是 内核 + 用户态联合管理的资源

内部有：

文件描述符 fd（内核分配）

缓冲区 buffer（用户态管理）

文件位置指针（当前读写位置）

状态标志（EOF、错误标志）

所以深拷贝 FILE* 本质上是 无法实现 的。FILE类只能移动，不能拷贝
File 对象的拷贝不是数据拷贝，而是资源所有权拷贝，资源只能有一个主人

移动move操作之后，原来的资源全部转移，原指针指向nullptr，不是野指针

write操作不是直接写入内存，而是写进缓冲区，这样可以优化性能